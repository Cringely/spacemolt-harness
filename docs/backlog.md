# Backlog

> **Generated file — do not hand-edit.** The GitHub Issues in this repo are the source of truth for the backlog; this is a versioned, in-git VIEW of them, regenerated by `scripts/gen-backlog.py` (the doc-steward runs it after a merge cluster — see `docs/wiki/working-agreements.md`). It lives in the repo so the backlog is versioned and portable, not account-hosted. `[P]` = parked; size is S/M/L/XL.

_Regenerated from 48 open issues._


## Improv mode

- #118 — DIRECTION: hold improv Batches C/D/E until the base pilot has a VERIFIED economic win

## Pilot tuning

- #107 — Pilot milestone goal: rare-ore refit -> Mining Laser III (operator to confirm)
- #277 `M` — Deposit-location knowledge base: retain observed POI deposits + register query_intel, feed 'known deposits' into context
- #278 `M` — Conversational chat responses: answer questions directed at the pilot in local chat (greetings, resource-location)
- #367 — travel_to: direct POI-to-POI route fails ('no route') where multi-hop works
- #458 `M` — Value-aware BUY guard + teach mine_resource objectives: two market buys burned ~220k credits without progressing missions

## Observability

- #142 — Pilot economics: deterministic dashboard panel + codify the strategy-review role
- #183 `S` — Experiment: session-usage awareness â€” what signal does the headless subscription path actually expose?
- #262 — Counterfactual token accounting: measure the net sign, don't eyeball it (technique from pxpipe research)
- #415 `S` — deadman: no persistent log under cron â€” its own diagnostics vanish into cron's stdout

## Tech debt

- #137 `S` — Tighten HTTP mutation-retry safety (query-only retry, or don't retry post-commit-ambiguous cases)
- #245 `L` — Evaluate Headroom as a reversible compressor for game-API/tool-output JSON (the input we don't author)
- #453 `M` — Extract the stateful stall orchestration from agent.ts (StallMonitor phase 2)
- #478 `S` — Re-home compose-hardening drift-check to the private GitOps repo CI

## Fleet (parked)

- #159 `L` `[P]` — Playstyle personas: Explorer, Smuggler, and the space-opera role catalog (fleet content layer)
- #223 — EPIC: Combat & battle management â€” spacemolt_battle + hunt (Pirate Hunter path)
- #224 — EPIC: Salvage, wrecks & insurance â€” spacemolt_salvage
- #225 — EPIC: Drone autonomy â€” spacemolt_drone (Drone Pilot path)
- #226 `XL` — EPIC: Faction & facility empire â€” spacemolt_facility + spacemolt_faction (Base Builder path)
- #227 — EPIC: Passenger transit â€” list/load/unload passengers (Passenger Carrier path)
- #228 — EPIC: Fleet coordination â€” spacemolt_fleet
- #230 — Citizenship & empire relations â€” spacemolt_citizenship (4 actions, all unregistered)
- #231 — Social extras â€” spacemolt_social remainder (23 of 25 actions unregistered)
- #232 — Player-to-player transfer & storage gifting â€” spacemolt_transfer + spacemolt_storage

## Process

- #89 — Improv Batch C: improv loop + standing briefing
- #90 — Improv Batch D: backstops around the model + buy/mine effect-verify
- #91 — Improv Batch E: triggers/reversion + two-architecture eval
- #97 — Dashboard cost-figure relabel
- #98 — Schema-conformance client-parser gap
- #99 — Fleet: cooperating substrate (Spec 1)
- #100 — Fleet: negotiation (Spec 2)
- #101 — Fleet: empire (Spec 3)
- #102 — Sonnet-vs-Haiku A/B performance experiment
- #132 `S` — Wiki: consolidate game-mechanics reference from catalog.json + verified captures
- #166 `M` — Parallelism/cost package: unblock the critical path, tier the dispatches (council item 7)
- #193 `M` — Weekly burn-down protocol: consume to 96% in the final 12h â€” on a ranked queue, never manufactured
- #196 — CAPSTONE: extract the portable harness framework â€” the project's end deliverable
- #210 — Dependency-change-as-task CI gate (bun.lock) â€” #171 remainder
- #213 — EPIC: persona-derived goal ladders â€” the agent reads the game's docs and builds its own path to its persona's end-state
- #240 — Local planner via LM Studio (RX 7900 XTX, 24GB): take the pilot off the subscription quota
- #257 — Feasibility study: local LLM on the always-on planner host
- #264 — COUNCIL: re-examine every rejected tool for off-label uses (ponytail, Graphify, Headroom, pxpipe, cave*)
- #287 — Subagent exceeded brief: fork posted a PR review to GitHub under the shared identity without authorization
- #335 — Wave-E sweep 2 low-severity findings: codex classifier fragility, flag-pin absence gap, fixed workdir path, untested dashboard render JS
- #456 `S` — Guardrail: block subagent git state-changes in the MAIN checkout (2nd occurrence, prose didn't hold)
- #459 `S` — Scheduler self-heal can't deploy itself: cron runs the wrapper FROM the checkout it heals (32h outage, 2026-07-19/20)
- #466 `S` — Guardrail hook: deny 'gh pr merge' chained after other commands in one Bash call (3rd+ occurrence, PM this time)
- #476 — strategy-store: SM_STORE_URL unwired, step-0 gate ERRORs (exit 2), blocks every strategy review

## Recently done (closed)

- ~~#436 — spend-tally.ts renders as binary in git diffs (NUL byte in rowKey separator)~~
- ~~#431 — Transient game-server 5xx triggers a full replan per retry â€” add deterministic backoff-retry for blocked-by-5xx steps~~
- ~~#427 — mark route busy_timeout blocks the sync bun:sqlite event loop up to 5s on contention (low pri)~~
- ~~#424 — refresh-game-reference.ts makes live network calls + rewrites vendored docs by DEFAULT â€” should be safe-by-default~~
- ~~#414 — spend-tally calibration residual: ~4 gap vs /usage after worktree-dir fix~~
- ~~#413 — Headless steward left the scheduler-host shared checkout on a local branch - every tick since aborted at git pull (4h outage #2)~~
- ~~#410 — Scheduler run rows carry no model field â€” spend estimates zero on the subscription path~~
- ~~#402 — Pilot fumbles module upgrades: install_mod called for a module not in cargo (Mining Laser III, 2x)~~
- ~~#368 — scan action: 100% lifetime failure (27/27, invalid_target) -- dormant broken capability~~
- ~~#366 — Digest lacks value signal in belt selection -- pilot parks at sub-floor ore belt despite 'sell profitably' steer~~
- ~~#358 — docs: steward queue â€” 2026-07-17 audit findings (stale pointers, M-40 gap, README trim, backlog mojibake)~~
- ~~#355 — Operator instructions persist in storage but lose salience after one wake â€” superseded in behavior, not satisfied~~
- ~~#349 — cargo_coherence scorer ignores CandidateStep.repeat/.until â€” under-counts removals~~
- ~~#348 — No-buyer damper: after N consecutive no-buyer results for the same held item, nudge list/relocate instead of re-searching~~
- ~~#330 — Mission-objective type blindness: deposit-check applies mining pressure to non-mining objectives~~
- ~~#326 — refresh-game-reference.ts wipes dated correction notes on re-capture~~
- ~~#322 — Register cancel_order + decide buy-side escrow posture: credits locked in standing bids have no deterministic reclaim path~~
- ~~#321 — steward-prep writes generated files to the main checkout when run from a worktree~~
- ~~#316 — Register create_buy_order: the game's named remedy when no seller exists (blocks #216's milestone)~~
- ~~#315 — estimate_purchase requires docked (live-falsified reference + 15 error events/window): dated doc correction + gate decision~~
