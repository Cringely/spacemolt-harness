# STATE.md Historical Archive (2026-07-13)

Archived sections from docs/STATE.md. Current state is in the main file.

## Done (chronological) — Plan 1 era

- Research + approved spec: `docs/superpowers/specs/2026-07-10-spacemolt-harness-design.md` (incl. Plan 2 additions: reflex policies, route macro, digest templates)
- Plan 1 (12 TDD tasks, full code): `docs/superpowers/plans/2026-07-10-core-engine.md`
- Process infrastructure: decision log, context system (AGENTS.md + wiki), team structure with model tiering, educational register, simplicity rules imported from a prior project, vendored tooling (`.claude/`), protected main + PR workflow, conditional PR-stage review policy
- **Batches A–D shipped** (PRs #1, #3 + pre-workflow commits): registry (16 actions) + conformance test, fake server + HTTP transport + client, store + executor + wake + mock planner, agent loop + config/bootstrap/e2e/main. All-Haiku implementers except Task 12 (Sonnet by lead's call). One arbitrated plan defect (impossible e2e assertion), triple-verified, one-line fix.
- **Council gate: PASS** — found 2 coverage holes, a compound silent-degradation risk on the fuel/hull safety path, 2 missing receipts; produced the trace-before-assert plan-authoring rule.
- **Gate fixes merged (PR #6)**: 68 tests, `status_error` event, receipts, fixture freshness check (fixture regenerated: 282 paths, no drift). First live PR-stage review: zero findings.
- **Task 1 spike: GO** (`docs/superpowers/specs/spike-claude-container-auth.md`): headless Claude authenticates in a container via `CLAUDE_CODE_OAUTH_TOKEN` (token in `secrets/claude_oauth_token`), no writable Claude home needed. Per-call overhead ~3k input tokens + cached prefix. `claude-spike` image left on the staging host for Plan 2 testing.
- **Flights SM-1/SM-2 flown and landed** (user go given): all engineered systems passed in the field. SM-2 diagnosis: both ASSUMED query shapes (get_system, find_route) were wrong; ground truth captured live (fixtures in test/client.test.ts and test/executor.test.ts, VERIFIED 2026-07-10) — get_system nests under `system`, find_route's route is `[{jumps,name,system_id}]` objects. Fixed with surroundings_error visibility so silent parse-degrade can't hide again. Fun fact from the capture: Haven (start system) has an asteroid belt (commerce_fields) — the pilot never needed to leave, just to see.
- **auto_list escape for unsellable cargo** (2026-07-12): a live miner mined 7x Palladium Ore no reachable NPC exchange buys and looped 10+ times bouncing between exchanges — structurally trapped by the registry. Fix (smallest, producer-side): registered `sell`'s optional `auto_list` param + digest briefing "on a no-buyers sell, retry the same sell with `auto_list=true` to list the goods on the player exchange, don't re-hunt NPC exchanges." Paired improv-mode briefing rule added. Deterministic executor auto-retry (option B) DEFERRED per "prove the premise before replacing a mechanism"; **promotion condition on record**: if the pilot again hits a no-buyers sell result and does NOT plan a follow-up `auto_list=true` sell (re-tries plain sell / travels to another NPC exchange for the same item), promote B to a deterministic §5 backstop. See decisions.md "Escaping unsellable cargo: auto_list". 380 offline tests.
- **auto_list falsified live → jettison escape** (2026-07-12): the auto_list fix above (PR #75) rested on an unverified premise. Live test disproved it — the pilot did `sell palladium_ore auto_list:true` and the game returned the SAME "Sold 0 … 10 unsold (no buyers)". auto_list does NOT list/clear no-demand cargo. Correction shipped: (1) digest briefing rewritten honestly — auto_list does NOT reliably clear a no-demand item, don't loop; (2) registered core `jettison(id, quantity)` as the GUARANTEED disposal escape (depends on no external demand) + digest brief "if it can't be sold, jettison it to free the hold"; (3) paired improv-mode rule updated. `create_sell_order` with explicit price (the ACTUAL player-exchange listing mechanism) noted as the value-preserving upgrade (DEFERRED — needs price logic + a no-demand item may never fill). See decisions.md "Disposing of no-demand cargo: jettison (auto_list falsified live)". This is fix-quality's "prove the premise" in action: shipped on a guess, live-tested, guess failed, corrected. 381 offline tests.
- **Flight campaign SM-3/SM-4** (user go standing): F-1 verified in field (map-aware plans name real POIs); name→id normalization added at plan admission (PR #21); **FIRST ORE MINED** (SM-4, commerce_fields, 1 yield — Haven's belt then confirmed depleted); two new gaps found live and fixed: current-location blindness when undocked (get_system's poi now wired to digest "You are at:") and a thrash-damper bypass via no-op travel (trigger replaced with block-reason repetition). Next: SM-5 — expected first full mine→sell loop in a fresh system.

## Blockers / waiting on user — Plan 2 era

- Registration code from https://spacemolt.com/dashboard → `secrets/registration_code` (needed before the first live run, not before Plan 2/3 build).
- **Plan 2 authored, reviewed, merged (PR #9)**: `docs/superpowers/plans/2026-07-10-real-planners.md`, 9 tasks, batches E (1-3) / F (4-6, first-flight checkpoint) / G (7-9). Review fixed 2 blockers; security audit's PlanSchema-enforcement mandate folded into Task 4. Load-bearing unknowns declared and isolated: find_route shape, token-invalid error shape, cooldown default.
- **Security baseline established** (`docs/wiki/security-baseline.md`): audit-driven; git history verified secret-free; Plan 4 hardening constraints written into the spec.
- **Registration code + Claude token in secrets/** — no user-blockers remain.
- **User decisions recorded**: improv mode (future, see decision log); dashboard via the reverse proxy behind the SSO provider's forwardAuth (Plan 4 constraint); operations doc required (Plan 3/4); no live LLM/game calls by agents without PM authorization (AGENTS.md).

## Operating mode / Batch execution — Plan 2/3 era

Autonomous execution authorized through first-flight READINESS: Batch E → F → G sequentially, each gated on the previous report. HARD STOP before the first live flight (agents playing on the real game) — that needs the user's explicit go. If subscription limits close mid-pipeline: stop dispatching, update this file, resume next window.

- **Batch E shipped via PR #12** (commits 3f0ddaf, 28a175d, 9254f69): runner seam, claude-subscription planner, ollama planner. 89 tests. NOT-clean batch, PR-stage reviewed: two process deviations + the b@2.0.1 chain-integrity incident (see decision log) — halted, remediated, three new standing policies.
- **Security function established** (user-directed): policy authority + audit triggers + incident halt authority — team-structure.md and security-baseline.md.
- **Batch F shipped via PR #14** (b0b4b73, 96b2dba, 01f7a37): failure classification, PlanSchema gating at the replan seam, config growth, real-planner wiring. 107 tests. PR-stage review (safety-path seam): zero findings.

## FIRST-FLIGHT READY — Plan 3 era

`bun run src/main.ts` with a real agents.yaml now: registers/logs in agents, runs the full loop with real planners (claude-subscription via token, ollama, mock), classified failure handling, PlanSchema-gated plans. Unverified until a live run (declared, isolated): claude CLI error-classification regexes; ollama structured-output subset vs a real server; find_route response shape (Batch G). The live flight itself REQUIRES the user's explicit go.

- **Batch G shipped via PR #16** (0acb321, c5fdb14, dcd8623, 6d7f736): digest wiring, reflex policies, travel_to macro. 129 tests. **Plan 2 complete** (market math deferred by design, on the record).
- **Plan 2 council gate: PASS on code, 4 readiness fixes ordered** (see decision log). Security gate audit: zero findings, all post-incident policies held.
- **Token-invalid classification VERIFIED against real CLI output** (bogus-token probe, clean container, zero usage): structured envelope with api_error_status 401 — classifier hardened to key on it (fix/classifier-ground-truth).
- **FLIGHT CAMPAIGN SM-1..SM-5 complete** (see decision log entry): first ore mined, first autonomous jump, five failure classes fixed permanently (PRs #19-#22), damper verified live. Loop not yet closed (no sale) — Haiku strategically scattered; persona/model-tier tuning is the next lever. Remaining unknowns (429 shape, Ollama subset, cooldown) discharge in longer flights.
- **Plans 3+4**: dashboard LIVE at http://127.0.0.1:8642 (SpaceMolt-branded Mission Control, PRs #24-#27); Plan 4 (container/reverse-proxy/SSO) authored+reviewed+merged (PR #28), execution pending — 10 operator-confirm items in the plan.
- **SM-8 EXPERIMENT CONCLUSIVE**: Haiku + salience fixes (cargo manifest, goal carryover, sell guidance — PR #29) sold within 2 minutes and kept selling. Briefing was the gap, not model tier. Miner flies cheap; pilot online continuously (standing policy).
- **CONTAINER CI GREEN (overnight)**: PRs #32-#35 — social capabilities + injection armor merged (SM-10 flying); Batch J container merged with registry-API-resolved digest pins; GitHub Actions workflow live; **first successful run built the image (254-test gate inside the build) and published the container image to the registry (SHA tag + latest-main)**. The prototype container exists, proven in CI.
- **BLOCKED overnight, resumes with the operator**: staging deploy + digest-verify-on-host — the secret vault was locked, SSH signing unavailable (correctly not bypassed). A monitoring token was found hardcoded in temporary scripts outside the repo — rotation recommended (user item).

## (earlier) DEPLOYED TO THE TARGET HOST (2026-07-11)

Pilot (Rockhopper Kess) runs on the staging host (a dedicated VM), NOT this workstation. Dashboard: http://<staging-host-LAN>:8642 — SpaceMolt Command Center app-shell (sidebar // Fleet / // Monitoring, TELEMETRY rail, live ship vitals: credits/system/fuel/hull/cargo) — operator-approved "significantly improved". Full CI pipeline live: push to main -> Actions builds+publishes the container image -> host `docker login` (PAT in secrets/ghcr_pat) + pull + compose up. Deploy quirks solved + documented: private-repo code shipped via `git archive`|ssh; db_path /app/data; bind-mount secrets chmod 0644 (non-root container UID); dashboard_host 0.0.0.0 + published on LAN IP; registry App tokens can't read private user packages (PAT works).

## MORNING RUNBOOK (superseded by production deploy)

1. Unlock the secret vault (restores SSH signing).
2. Staging deploy: on the staging host pull the repo, `cp .env.staging.example .env` (set HARNESS_IMAGE_TAG to the latest 7-char SHA from the registry or build on host), provision secrets/ (claude_oauth_token, registration_code, miner_password from this workstation), agents.yaml with dashboard_host: 0.0.0.0, STOP the local harness first (single game session per account!), then `docker compose -f docker-compose.staging.yml up -d` and verify per docs/wiki/operations.md.
3. Optional: registry read token on staging for registry pulls (else build-on-host works).
4. SSO provider UI (~10 min): application + provider for the dashboard's internal domain using the existing SSO middleware; then the NAS-host promotion per the production compose file whenever staging satisfies.
5. Rotate the exposed monitoring token (temporary scripts) and gitignore/delete those files.

## Next — stale queue

1. Execute Plan 4 batches J (image+compose) and K (deploy to the staging host) — operator-confirm items need the user (reverse-proxy labels, domain, staging-host SSH entry, digest resolution). 2. Fleet growth: scout + corsair on haiku with salience digests (SM-8 result). 3. Longer unattended flights; 429/cooldown unknowns discharge via the dashboard. (Superseded list: 1. Author Plan 3 (dashboard) — named requirements: sticky-flag observability + recovery, instruction box bound, ops doc (docs/wiki/operations.md + README quickstart), wake-reason histogram (the persona-tuning instrument the campaign proved necessary), usage metering. Reverse-proxy/SSO constraints in spec for Plan 4.
2. Quick experiment next flight: miner replans on sonnet (config change only) — test whether strategic scatter is model-tier.
3. Longer unattended flight once the dashboard lands (damper + all bounds verified live; safe to leave running).

## Plan sequence (from spec)

Plan 1: core engine (**done**) → Plan 2: real planners → Plan 3: dashboard (React/Vite, REST+WS, instruction box) → Plan 4: container + first live run.
