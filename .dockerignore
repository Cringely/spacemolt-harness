secrets/
harness.sqlite
harness.sqlite-*
node_modules/
.git/
docs/
spike/
# .claude/ is excluded EXCEPT the three test inputs (the complete set — grep
# test/ for ".claude/" when changing this): agent-worktree-gate.test.ts imports
# hooks/ and reads agents/*.md tool grants; guardrails-slice.test.ts reads
# guardrails.md + hooks/session-start-guardrails.sh (its skip-gate checks the
# .claude DIR, so a partial tree makes it fail loudly by design). The
# L-20/#130 vendored-file-outside-copy-path class.
.claude/*
!.claude/hooks/
!.claude/agents/
!.claude/guardrails.md
*.md
.env
agents.yaml
