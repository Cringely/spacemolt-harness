{
  "name": "spacemolt-harness",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": { "bun": ">=1.2.21" },
  "scripts": {
    "start": "bun run src/main.ts",
    "test": "bun test",
    "typecheck": "tsc --noEmit",
    "preflight": "bun run scripts/preflight.ts",
    "eval:planner": "bun run src/eval/run.ts"
  },
  "dependencies": {
    "zod": "^3.24.0"
  },
  "devDependencies": {
    "@types/bun": "^1.2.21",
    "typescript": "^5.7.0"
  }
}
